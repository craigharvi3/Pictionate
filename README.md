# Pictionate
Picture guessing game built with React

## Setup

**Install dependencies**

```
brew update
brew install yarn
```

**Get up & running**

```
yarn install
npm start
```
Then visit http://localhost:8080/ (woohoo!)
